import { CheckIdProps } from '../types/utils';
export declare const checkId: ({ reportLackId, reportMaxValidLengthExceeded, hasId, currentIdValue, translationObjectKey, generateId, namespaceMatchers, context, node, maxValidLength, invalidCharsPattern, invalidCharsReplacement, invalidCharsReplacer, }: Omit<CheckIdProps, "idName">) => void;
